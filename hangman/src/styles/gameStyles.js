import { 
    StyleSheet, 
} from 'react-native';

/**
 * css stylesheet for the main game screen 
 */

export default StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
      },
      
});
  